import styled from "styled-components";

const HeaderWrapper = styled.div`
  display: flex;
  flex-direction: row;
  text-align: center;

  .about {
    padding-top: 3px;
  }
`

export default HeaderWrapper;